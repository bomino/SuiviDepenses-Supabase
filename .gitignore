# local config (Supabase URL + anon key go in index.html directly,
# but if you ever externalize them: keep .env files out)
.env
.env.local

# editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code per-user settings (project-shared settings.json may be committed)
.claude/settings.local.json

# Supabase CLI working dir
supabase/.temp/

# Local screenshots dropped into the repo root (not part of the project)
Screenshot.png

# Railway / Heroku log exports
logs*.json

# python (only here for the optional icon regenerator)
__pycache__/
*.pyc
.venv/
venv/
